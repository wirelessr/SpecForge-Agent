# Environment Configuration Example
# Copy this file to .env.test (for unit tests) or .env.integration (for integration tests)
# Then edit the values appropriately for each environment

# LLM Configuration
LLM_BASE_URL=http://your-llm-server.local:8888/openai/v1
LLM_MODEL=models/your-model-name
LLM_API_KEY=your-actual-api-key-here
LLM_TEMPERATURE=0.7
LLM_MAX_OUTPUT_TOKENS=4096
LLM_TIMEOUT_SECONDS=60

# Framework Configuration
WORKSPACE_PATH=workspace
LOG_LEVEL=DEBUG
SHELL_TIMEOUT_SECONDS=30
SHELL_MAX_RETRIES=2

# For unit tests (.env.test), use test values:
# LLM_BASE_URL=http://test.local:8888/openai/v1
# LLM_MODEL=models/test-model
# LLM_API_KEY=test-key-for-development

# For integration tests (.env.integration), use real values:
# LLM_BASE_URL=http://ctwuhome.local:8888/openai/v1
# LLM_MODEL=models/gemini-2.0-flash
# LLM_API_KEY=sk-your-real-api-key